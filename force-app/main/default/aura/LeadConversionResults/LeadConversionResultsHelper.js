({
	/*getConvertedAccountJS : function(component,event) {
        component.set("v.loadSpinner",true);
		var accAction = component.get('c.getConvertedAccount');
        accAction.setParams({
            "accId" : component.get("v.convertedAccountId")
        });
		accAction.setCallback(this, function(result) {
            component.set("v.loadSpinner",false);
			if(result.getReturnValue() != null) {
				component.set('v.convertedAccount',result.getReturnValue());
			}
		});
        $A.enqueueAction(accAction);
	},
    
    getConvertedOpportunityJS : function(component,event) {
        component.set("v.loadSpinner",true);
		var oppAction = component.get('c.getConvertedOpportunity');
        oppAction.setParams({
            "oppId" : component.get("v.convertedOpptyId")
        });
		oppAction.setCallback(this, function(result) {
            component.set("v.loadSpinner",false);
			if(result.getReturnValue() != null) {
				component.set('v.convertedOppty',result.getReturnValue());
			}
		});
        $A.enqueueAction(oppAction);
	},
    
    getConvertedContactJS : function(component, event, helper) {
        component.set("v.loadSpinner",true);
		var contAction = component.get("c.getConvertedContact");
        contAction.setParams({
            contId : component.get("v.convertedContactId")
        });
		contAction.setCallback(this, function(result) {
            component.set("v.loadSpinner",false);
			if(result.getReturnValue() != null) {
				component.set("v.convertedContact",result.getReturnValue());
			}
		});
        $A.enqueueAction(contAction);
	}*/
})