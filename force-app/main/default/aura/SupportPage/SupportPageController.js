({
    
    handleClick : function(component, event, helper) {
        helper.openPopup(component);
    },
    
    handleDownload : function(component, event, helper) {
        helper.downloadForm(component);
    },
})